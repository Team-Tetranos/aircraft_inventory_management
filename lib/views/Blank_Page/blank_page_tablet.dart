import 'package:flutter/material.dart';

class BlankPageTablet extends StatelessWidget {
  const BlankPageTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}
