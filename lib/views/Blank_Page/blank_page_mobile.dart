import 'package:flutter/material.dart';

class BlankPageMobile extends StatelessWidget {
  const BlankPageMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.yellow,
    );
  }
}
