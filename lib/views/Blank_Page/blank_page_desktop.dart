import 'package:flutter/material.dart';

class BlankPageDesktop extends StatelessWidget {
  const BlankPageDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
